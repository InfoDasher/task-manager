/**
 * Reusable icon components
 * Re-exporting Lucide icons with consistent interface
 */

import { Eye, EyeOff, Loader2, FolderOpen, CheckCircle, Clock, List, Code } from "lucide-react";

// Re-export Lucide icons with consistent naming
export { Eye as EyeIcon, EyeOff as EyeSlashIcon, Loader2 as LoadingSpinner, FolderOpen as FolderIcon, CheckCircle as CheckCircleIcon, Clock as ClockIcon, List as ListIcon, Code as CodeIcon };
